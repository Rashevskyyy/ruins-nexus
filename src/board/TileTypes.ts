export enum TileType {
    LandingHub = "LandingHub", // Center tile - shared trading hub
    Empty = "Empty",
    Resource = "Resource",
    FinalTile = "FinalTile",
    Base = "Base", // Player's base (was City)
    StartingSector = "StartingSector", // v0.5: Player's starting zone (has resources, is home zone)
}
